import React from 'react';

export default function SavedColors() {
  // can be a dropdown with some divs - 5?
  // divs can have a background color of the saved color
  // foreground text can be the name
  // foreground text can be black or white, depending on average of background rgb
  
  return (<>
    
  </>)
}